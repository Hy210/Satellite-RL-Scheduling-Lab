# AGENTS.md

## 프로젝트 목적

이 프로젝트는 강화학습을 이용한 단일 위성 촬영 스케줄링 프로토타입을 구현하는 동시에, 사용자가 강화학습의 개념, 로직 및 알고리즘을 학습하고 이해도를 높이는 것을 목적으로 한다.

프로젝트 작업 시 다음 문서를 기준으로 한다.

- [RL 스케줄링 설계](docs/rl-scheduling-design.md)
- [웹 애플리케이션 설계](docs/web-application-design.md)
- [단계별 구현 계획](docs/implementation-plan.md)
- [개인 RL 학습 노트](docs/rl-study-notes.md)
- [프로젝트 지식 베이스](docs/project-knowledge.md)
- [현재 작업 인계](HANDOFF.md)
- [데이터 형식](docs/data-format.md)

## 세션 시작 규칙

새로운 세션에서 프로젝트 작업을 시작할 때 다음 순서로 현재 상태를 확인한다.

1. `AGENTS.md`를 읽어 프로젝트 규칙을 확인한다.
2. `HANDOFF.md`를 읽어 마지막 작업 상태, 테스트 결과, blocker와 다음 작업을 확인한다.
3. 현재 파일 및 Git 상태가 handoff 내용과 일치하는지 확인한다.
4. 관련 설계 문서와 구현 계획의 해당 단계만 추가로 읽는다.
5. handoff의 다음 작업이 사용자의 최신 요청과 충돌하면 최신 요청을 우선하고 handoff를 갱신한다.

## 구현 진행 규칙

- `docs/implementation-plan.md`의 단계와 완료 조건을 따른다.
- 이전 단계의 테스트와 완료 조건을 충족한 뒤 다음 단계로 이동한다.
- 설계 또는 구현 변경이 발생하면 아래의 문서 자동 동기화 규칙에 따라 관련 문서를 같은 작업 안에서 갱신한다.
- RL core, Backend 및 Frontend의 책임 경계를 유지한다.
- 사용자가 검토나 설명만 요청하면 명시적인 구현 요청 없이 코드를 변경하지 않는다.

## 코드 설명 및 한글 주석 규칙

코드는 사용자가 구현과 RL 개념을 함께 이해할 수 있도록 간결한 한글 설명을 포함한다.

- 모듈, 주요 클래스 및 공개 함수에는 역할을 설명하는 한글 docstring을 작성한다.
- RL 개념, 도메인 규칙, 수식, 상태 전이 및 구현 이유가 바로 드러나지 않는 부분에는 한글 주석을 작성한다.
- 주석은 코드가 `무엇을 하는지`만 반복하지 않고 `왜 필요한지`, `어떤 개념에 대응하는지`를 우선 설명한다.
- 변수명, 함수명, 클래스명은 일반적인 Python/TypeScript 관례에 맞는 영어를 유지한다.
- 자명한 대입, 반복문 또는 문법을 줄마다 번역하듯 주석 처리하지 않는다.
- 코드와 어긋난 오래된 주석을 남기지 않으며 동작 변경 시 관련 주석과 docstring도 함께 갱신한다.
- 테스트에는 검증 의도가 이름만으로 불명확할 때만 짧은 한글 주석을 추가한다.
- 외부 라이브러리의 표준 용어는 `action mask`, `episode`, `policy`처럼 영문을 함께 사용할 수 있다.

## 문서 자동 동기화 규칙

설계 결정, 구현 방식, 외부 인터페이스 또는 개발 절차가 변경되면 별도의 문서화 요청을 기다리지 않고 관련 문서를 자동으로 갱신한다. 코드 변경과 문서 변경은 가능한 한 같은 작업에서 완료한다.

사용자가 명시적으로 문서를 변경하지 말라고 요청한 경우에는 문서를 수정하지 않고, 최종 응답에서 갱신이 필요한 문서를 알려준다.

### 변경 유형별 대상 문서

| 변경 내용 | 반드시 검토하고 갱신할 문서 |
|---|---|
| State, action, reward, episode 또는 RL 알고리즘 | `docs/rl-scheduling-design.md` |
| 주문, strip, opportunity, 자세 및 시간 제약 | `docs/rl-scheduling-design.md` |
| 화면, 사용자 흐름, API 영역, 저장 또는 worker 구조 | `docs/web-application-design.md` |
| 구현 순서, 단계 범위, 테스트 또는 완료 조건 | `docs/implementation-plan.md` |
| 사용자가 질문하거나 학습한 RL 개념 | `docs/rl-study-notes.md` |
| 위성 도메인, 스케줄링, 데이터 및 시스템에 관해 새로 확인한 지식 | `docs/project-knowledge.md` |
| 현재 작업 상태, 검증 결과, blocker 및 다음 작업 | `HANDOFF.md` |
| 실제 데이터 필드, 단위, JSON 형식 또는 검증 경계 | `docs/data-format.md` |
| 프로젝트 전체 작업 규칙과 문서 관리 정책 | `AGENTS.md` |

하나의 변경이 여러 영역에 영향을 주면 관련 문서를 모두 갱신한다. 예를 들어 action 구조가 바뀌어 API 응답과 재생 화면도 달라지면 RL 설계, 웹 설계 및 구현 계획을 함께 검토한다.

### 설계 레벨 변경

다음 항목은 설계 레벨 변경으로 취급한다.

- 기존에 확정한 동작이나 수식의 변경
- 데이터 모델, 책임 경계 또는 시스템 구조 변경
- 새로운 기능 범위 추가 또는 기존 범위 제거
- 알고리즘, 라이브러리 또는 저장 방식의 중요한 선택
- 제한값, 기본 파라미터 또는 평가 기준 변경

설계 레벨 변경 시 다음 순서를 따른다.

```text
변경 이유와 영향 범위 확인
-> 관련 설계 문서 갱신
-> 구현 계획과 테스트 영향 검토
-> 코드 변경
-> 문서와 실제 동작의 일치 확인
```

사용자가 설계 검토만 요청하고 실제 적용을 요청하지 않은 경우에는 문서를 자동으로 확정 변경하지 않는다. 사용자가 제안을 수락하거나 변경을 지시한 시점에 문서에 반영한다.

### 구현 레벨 변경

다음 항목은 구현 레벨 변경으로 취급한다.

- 실제 디렉터리 또는 모듈 구조 확정
- 실행, 빌드, 테스트 및 개발 명령 변경
- API endpoint와 request/response schema 확정
- 데이터 파일 및 데이터베이스 schema 확정
- 설정 이름, 환경변수 또는 기본값 변경
- 구현 계획 단계의 작업 완료
- 설계와 다르게 구현할 필요가 생긴 경우

구현 레벨 변경 시 다음 규칙을 적용한다.

- 구현 세부사항이 기존 설계와 일치하면 관련 문서에 실제 경로, 명령 또는 확정 형식을 보완한다.
- 구현이 설계를 변경한다면 임의로 코드만 바꾸지 않고 설계 레벨 변경 절차를 적용한다.
- `docs/implementation-plan.md`의 체크박스는 실제 구현과 검증이 완료된 항목만 체크한다.
- 테스트를 작성했지만 통과하지 못한 항목은 완료로 표시하지 않는다.
- 단계의 완료 조건을 모두 만족하기 전에는 해당 단계를 완료했다고 기록하지 않는다.
- 임시 우회 구현은 확정 설계처럼 기록하지 않고 알려진 제한사항 또는 후속 작업으로 명시한다.

### 문서 갱신이 필요하지 않은 변경

다음 변경은 동작, 인터페이스 또는 개발 방법에 영향을 주지 않는다면 문서 갱신을 생략할 수 있다.

- 내부 변수명 정리
- 동작이 동일한 단순 리팩터링
- 주석과 오탈자 수정
- 테스트 데이터의 의미 없는 값 변경
- format 또는 lint에 의한 기계적 변경

판단이 애매하면 문서가 필요한 쪽으로 처리한다.

### 작업 완료 전 확인

설계 또는 구현을 변경한 작업은 최종 응답 전에 다음을 확인한다.

```text
[ ] 변경된 동작과 영향을 받는 문서를 식별했는가?
[ ] 관련 설계 문서가 실제 구현과 일치하는가?
[ ] 구현 계획의 작업 및 완료 상태가 사실과 일치하는가?
[ ] 새 API, 파일 형식, 설정 또는 실행법이 기록되었는가?
[ ] RL 학습 설명이 있었다면 개인 학습 노트를 갱신했는가?
[ ] 문서 링크가 유효한가?
```

최종 응답에는 코드 변경과 함께 갱신한 문서를 간단히 알린다.

## Handoff 자동 갱신 규칙

[HANDOFF.md](HANDOFF.md)는 새 세션이 이전 작업을 자연스럽게 이어가기 위한 최신 상태 snapshot이다. 작업 이력을 길게 누적하는 일지가 아니라 현재 상태만 간결하고 정확하게 유지한다.

다음 시점에 별도 요청을 기다리지 않고 handoff를 갱신한다.

- 구현 또는 문서 작업의 의미 있는 단위가 끝났을 때
- 작업이 중단되거나 blocker가 발생했을 때
- 설계, 데이터 계약 또는 외부 인터페이스가 변경됐을 때
- 테스트 결과나 알려진 오류 상태가 바뀌었을 때
- 구현 계획의 현재 단계 또는 다음 작업이 바뀌었을 때
- 사용자에게 작업 완료 결과를 전달하기 직전

Handoff에는 마지막 갱신일, 현재 목표와 구현 단계, 완료 및 진행 중 작업, 주요 변경 파일, 검증 결과, blocker, 미확정 사항, 다음 세션의 첫 작업, 작업 디렉터리, Git 상태와 관련 문서 링크를 유지한다.

### 갱신 원칙

- 완료하지 않은 작업을 완료로 기록하지 않는다.
- 실행하지 않은 테스트를 통과했다고 기록하지 않는다.
- 성공적으로 끝난 작업은 `진행 중 작업: 없음`으로 명확히 표시한다.
- 다음 작업은 가능한 한 하나의 구체적인 시작점으로 작성한다.
- 오래된 진행 상황은 삭제하거나 현재 상태로 교체한다.
- 상세 설계나 학습 내용을 복제하지 않고 원본 문서에 링크한다.
- Git 저장소가 아니거나 상태를 확인할 수 없으면 그 사실을 명시한다.
- 강제 종료 가능성이 있는 장시간 작업에서는 중요한 작업 단위가 끝날 때 중간 갱신한다.
- 사용자가 명시적으로 handoff를 변경하지 말라고 요청하면 수정하지 않고 최종 응답에서 필요한 갱신 내용을 알린다.

## 프로젝트 지식 베이스 자동 갱신 규칙

[프로젝트 지식 베이스](docs/project-knowledge.md)는 프로젝트를 수행하면서 축적한 도메인 지식, 개념적 이해, 설계 근거 및 발견 사항을 보존하는 문서다. 개인의 RL 학습 과정을 기록하는 `docs/rl-study-notes.md`와 목적을 구분한다.

다음 내용이 새롭게 확인되거나 기존 이해가 바뀌면 별도의 기록 요청을 기다리지 않고 같은 작업 안에서 프로젝트 지식 베이스를 갱신한다.

- 위성 궤도, pass, 자세, 기동 및 촬영과 관련된 도메인 지식
- 주문 영역, strip, 촬영 기회 및 스케줄링에 관한 개념
- RL 환경으로 단순화하면서 유지하거나 제외한 현실 제약과 그 이유
- 데이터 생성, 정규화, 검증 및 품질에 관한 발견
- 알고리즘 또는 시스템 구조를 선택한 이유와 trade-off
- 실험, 디버깅 또는 평가 과정에서 확인한 재사용 가능한 지식
- 기존 가정이 잘못됐거나 불충분하다는 사실
- 향후 기능 확장 시 다시 검토해야 할 도메인 위험

### 기록하지 않는 내용

- 파일 한 개의 단순 수정 내역
- 구현 계획의 진행 상황만 나타내는 내용
- 특정 실행에서만 의미 있는 일회성 로그
- 근거 없이 추측한 도메인 사실
- 설계 문서에 이미 충분히 표현된 숫자나 API를 그대로 복사한 내용

### 지식 상태 구분

각 항목은 가능한 경우 다음 상태 중 하나를 표시한다.

- `확정`: 프로젝트에서 검증하거나 사용자와 합의한 지식
- `가정`: 프로토타입 단순화를 위해 임시로 채택한 내용
- `관찰`: 구현, 실험 또는 평가에서 확인했지만 일반화가 필요한 내용
- `검토 필요`: 근거 확인이나 추가 결정이 필요한 내용
- `폐기`: 이전에 사용했지만 더 이상 유효하지 않은 내용

외부 자료로 확인한 사실은 가능한 경우 출처 링크와 확인일을 남긴다. 프로젝트 내부의 설계 결정은 관련 설계 문서 링크를 남긴다. 사실과 프로젝트 편의상 선택한 가정을 같은 것으로 표현하지 않는다.

### 갱신 방식

- 기존 항목과 같은 주제라면 새 항목을 중복 생성하지 않고 기존 내용을 보완한다.
- 기존 지식이 변경되면 이전 내용을 조용히 삭제하지 않고 상태, 변경 이유 및 날짜를 기록한다.
- 확정 설계 자체는 관련 설계 문서에 기록하고, 지식 베이스에는 그 배경과 의미를 요약한다.
- 새 용어가 등장하면 용어집을 갱신한다.
- 새 위험이나 미해결 질문이 생기면 해당 목록을 갱신하고, 해결되면 상태를 변경한다.
- 사용자가 명시적으로 기록하지 말라고 요청한 경우에는 갱신하지 않는다.

## RL 학습 지원 규칙

사용자가 다음 내용을 질문하거나 구현 과정에서 해당 내용을 설명할 필요가 생기면 학습 지원 대상으로 취급한다.

- 강화학습의 기본 개념과 용어
- MDP, state, action, reward, transition, policy 및 return
- episode, step, trajectory, discount factor
- value function, Q-function 및 advantage
- exploration과 exploitation
- policy gradient, PPO 및 Maskable PPO
- action masking
- reward shaping과 보상 설계
- observation 및 action space 설계
- 학습 안정성, 평가, 과적합 및 일반화
- baseline, 최적화 알고리즘 및 RL 결과 비교
- 이 프로젝트의 RL 환경과 관련된 알고리즘 또는 구현 로직

### 응답 방식

RL 학습 질문에는 가능한 경우 다음 순서로 설명한다.

1. 짧은 정의
2. 직관적인 의미
3. 필요한 수식 또는 알고리즘
4. 위성 촬영 스케줄링 프로젝트에 적용한 예
5. 흔한 오해 또는 주의점

사용자의 현재 이해 수준과 질문 범위에 맞춰 설명하며, 불필요하게 어려운 수학이나 용어를 먼저 제시하지 않는다. 수식을 사용할 때는 각 기호의 의미를 함께 설명한다.

### 자동 기록 규칙

RL 학습 질문에 답했거나 구현 과정에서 교육적으로 중요한 RL 개념을 설명한 경우, 같은 작업 안에서 [개인 RL 학습 노트](docs/rl-study-notes.md)에 내용을 자동으로 추가한다.

별도의 기록 요청이나 확인을 기다리지 않는다. 단, 다음 내용은 기록하지 않는다.

- 단순한 진행 상황 보고
- 파일 경로나 실행 명령만 설명한 내용
- 같은 개념을 새로운 정보 없이 반복한 답변
- RL과 직접 관련 없는 일반 웹 또는 도구 사용법

기존 항목과 내용이 중복되면 새 항목을 만들지 않고 기존 항목을 보완한다. 사용자가 명시적으로 기록하지 말라고 요청한 경우에는 기록하지 않는다.

### 학습 노트 형식

새 항목은 `docs/rl-study-notes.md`의 주제별 목차 아래에 기록한다. 각 항목은 다음 형식을 기본으로 한다.

```markdown
## 주제 이름

### 핵심 정의

개념의 짧고 정확한 정의

### 직관

쉬운 언어로 설명한 의미

### 프로젝트에서의 적용

위성 촬영 스케줄링 환경에 대응시킨 예

### 수식 또는 동작 과정

필요한 경우 수식, 의사 코드 또는 단계

### 주의할 점

흔한 오해, 설계상 trade-off 또는 확인할 사항

### 학습 기록

- YYYY-MM-DD: 질문 또는 학습한 내용의 짧은 요약
```

모든 항목에 불필요하게 동일한 소제목을 강제하지는 않는다. 개념을 이해하는 데 필요한 부분만 사용한다.

## 문서 관리 규칙

- 학습 노트는 사용자의 개인 공부를 위한 문서이므로 구현 명세와 섞지 않는다.
- 프로젝트 지식 베이스는 도메인과 프로젝트 전반의 축적 지식을 다루며 개인 학습 진도와 섞지 않는다.
- 확정된 시스템 동작은 설계 문서에 기록하고, 개념 설명과 학습 과정은 학습 노트에 기록한다.
- 새로 확인된 도메인 지식과 설계 근거는 프로젝트 지식 베이스에 기록한다.
- 학습 노트에서 프로젝트의 확정 설계를 언급할 때는 관련 설계 문서에 링크한다.
- 문서는 한국어로 작성하되 표준 영문 RL 용어를 괄호와 함께 사용할 수 있다.
- 날짜는 `YYYY-MM-DD` 형식으로 기록한다.
- 학습 노트의 기존 설명을 변경할 때 사용자의 개인 메모나 표현을 임의로 삭제하지 않는다.
